# 本地开发 & 部署说明

这是 `blog.fantay.cc` 的本地副本，与新服务器版本一致（已含域名 `blog.fantay.cc` 和页脚备案号 `湘ICP备2025111331号-2`）。

## 工作模式：代码本地、文章用后台

- **本地**：改主题、组件、布局、样式、配置（`src/` 下除 `src/content` 外的内容、`public/`、`*.config.ts`、`.env` 等）。
- **服务器**：文章通过 `https://blog.fantay.cc/fantay` 后台或 Obsidian 上传，直接写进服务器的 `src/content`。

> 部署脚本**不会**碰服务器上的 `src/content`、`data/`、`.env`，所以本地部署不会覆盖你在后台发的文章。

## 本地预览

```bash
cd sonnet-blog
npm run dev      # 启动开发服务器，默认 http://localhost:4321
```

> 本地用 Node 26 构建测试已通过；服务器是 Node 20，行为一致。

## 部署到服务器

改完代码后，一条命令推上去：

```bash
./deploy.sh            # 同步代码 -> 服务器 npm install -> 构建 -> 重启 PM2 -> 校验
./deploy.sh --dry-run  # 先预览会同步哪些文件，不改动服务器
```

部署做了什么：
1. `rsync` 推送代码（排除 `src/content`、`data/`、`.env`、`node_modules`、`dist`、`.git` 等）
2. 服务器上 `npm install && npm run build`
3. `pm2 restart blog`（**构建失败会中止，旧版本继续在线，不会把站点搞挂**）
4. curl 校验 `https://blog.fantay.cc` 与页脚备案号

## 免密说明

部署用专用密钥 `~/.ssh/blog_deploy`（已装到服务器 `authorized_keys`），无需输密码。

## 服务器关键信息

| 项 | 值 |
|---|---|
| 服务器 | `root@120.27.133.123` |
| 站点目录 | `/root/sonnet-blog` |
| 进程 | PM2 `blog`（`dist/server/entry.mjs`，127.0.0.1:4321） |
| 反代 | nginx → 127.0.0.1:4321 |
| SSL | Let's Encrypt（certbot 自动续期） |
| 后台 | `https://blog.fantay.cc/fantay` |
